# Review goal

Independently assess whether the reported AuraCall retained-browser repair is
coherent and whether the evidence is sufficient to accept the browser-driving
path. Return only the nonce-bound JSON verdict requested by the wrapper. Pass
only if the repair preserves fail-closed identity checks, avoids duplicate
browser launch, and demonstrates a real response through the retained ChatGPT
browser.
