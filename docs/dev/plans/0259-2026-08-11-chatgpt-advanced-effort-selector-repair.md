# ChatGPT Advanced Effort Selector Repair | 0259-2026-08-11

State: CLOSED
Lane: P01
Plan version: 3
Gate state: READY_FOR_EXPLICIT_USER_RETRY_GO
Goal execution state: REPAIR_COMPLETE_RETRY_NOT_STARTED

## Stable Goal Objective

Remove the exact pre-submission blocker from LitScout Declaration Campaign
Experiment 5: make AuraCall reliably traverse ChatGPT's current compact model
control into `Advanced` and then the sibling `Effort` submenu, select the
requested Sol effort, and fail closed if it cannot verify that effort. Validate
the repair without launching or attaching to a browser, then install the exact
committed build once without submitting a prompt.

## Current State

- LitScout Plan 0371 reached the exact ChatGPT account and Project with
  `chatgpt:sol-high`, selected the GPT-5.6 Sol family, and then stopped before
  attachment or submission with `Unable to find the Thinking time dropdown
  menu.`
- The terminal audit proves zero conversation creation, attachment upload,
  prompt submission, model response, provider call, or canonical LitScout DB
  mutation. The failed AuraCall session and browser evidence remain retained.
- The user explicitly requested that the browser be stopped so AuraCall could
  update. Fresh admission reports zero `wsl-chrome-3` profile processes and no
  listener on port 45015. Browser launch, attachment, navigation, and retry stay
  excluded until the user's separate explicit `go`.
- Current source already knows the live nested structure
  `Show advanced options -> Model/Effort`, but both selector paths concatenate
  visible text before the accessible label and then require the combined value
  to start with `show advanced options`. The retained live option shape exposes
  visible `Advanced` plus accessible `Show advanced options`, so that predicate
  can fail even though the semantic control is present.
- The deterministic repair is green. Focused selector coverage passes 26/26,
  the broader selector/config/runtime packet passes 119/119, typecheck and
  build pass, and the full suite passes 2,798 tests with 65 skipped.
- Execution deviation: the nominally provider-free full suite exercised its
  known browser reattach e2e and launched exact test-owned Chrome PID 98032 on
  port 45015 at `about:blank`. It made no ChatGPT submission and was not an
  Experiment 5 retry. The first root-only SIGTERM did not hold; an exact
  process-group SIGTERM then stopped PID 98032 and all descendants, restoring
  zero profile processes and an unbound port. The profile History bytes changed
  during that test launch and are retained rather than rewritten or cleaned.
- Commit `b27b0c995019bcbc604f425c32abad6cf180c984` is pushed to
  `origin/fix/chatgpt-advanced-effort-selector` and installed once. Source and
  installed `dist/` contain the same 510 files with aggregate manifest SHA-256
  `ab2fb590a69ed7c52291959f800148022dbcd463163c662a4c6561828deb96f7`.
  The installed API is active at PID 82312 with `NRestarts=0`, `/status` returns
  HTTP 200, and the required browser profile remains at zero processes with
  port 45015 unbound.

## Authority And Effect Boundary

- Owned branch/worktree:
  `fix/chatgpt-advanced-effort-selector` at
  `/home/ecochran76/workspace.local/auracall-plan0259-thinking-time`.
- Allowed writes: the narrow model/thinking selector implementation, focused
  tests, this plan, `ROADMAP.md`, `RUNBOOK.md`, `docs/dev/dev-journal.md`,
  `docs/dev-fixes-log.md`, and build output generated by normal validation.
- Allowed runtime effect after committed green validation: one user-scope
  AuraCall install and exact source/installed parity readback. An API restart is
  allowed only if the documented installer requires it for safe replacement.
- Excluded: browser launch/attach/navigation/click, ChatGPT submission, model or
  provider call, session retry, LitScout DB mutation, scheduler/completion
  control, evidence cleanup, model downgrade, effort omission, and alternate
  submission paths.

## Execution Packet

1. Preserve the retained Plan 0371 failure fingerprint and exact live control
   labels as the regression subject.
2. Add a red pure navigation test for visible `Advanced` plus accessible
   `Show advanced options`, including whitespace-free `ModelGPT-5.6 Sol` and
   `EffortLight` sibling rows.
3. Repair both model-family and thinking-effort navigation so each evaluates
   visible text and accessible labels independently, accepts the semantic
   compact `Advanced` control, and never confuses `Model`, `Effort`, or `Speed`.
4. Run focused selector/config/runtime tests, typecheck, build, lint/diff, and
   the plan/goal audits. Reconcile every failure before integration.
5. Commit and push the provider-free repair. Install that exact commit once,
   verify source/installed hashes and launcher version, and leave the browser
   profile stopped for the user's later explicit retry activation.

## Acceptance Criteria

- [x] A deterministic red reproduces the exact `Advanced` text plus
  `Show advanced options` accessible-label shape.
- [x] Model-family navigation and effort navigation both recognize that shape
  without broad substring clicks.
- [x] The effort path still requires a verified requested option and retains
  strict pre-submission failure on missing menu or option.
- [x] Focused selector/config/runtime tests, typecheck, build, lint/diff, plan
  audit, and goal-policy audit pass.
- [x] The committed repair is pushed and installed with exact source/install
  parity while the required browser profile and port remain absent.
- [x] Governed Experiment 5 browser/submission/provider/LitScout effects remain
  zero. The one unintended full-suite `about:blank` launch is fully attributed,
  its browser-state mutation is retained, and exact cleanup restores zero
  profile processes and an unbound port.

## Local Goal Bounds

- `max_source_repair_attempts: 2`
- `max_review_rework_cycles: 1`
- `max_installs: 1`
- `max_api_restarts: 1 if installer replacement requires it`
- `max_browser_launches: 0 (original bound; violated once by the known full-suite reattach e2e, reconciled without retroactive authority widening)`
- `max_browser_attaches: 0`
- `max_chatgpt_submissions: 0`
- `max_provider_calls: 0`
- `max_litscout_db_mutations: 0`
- `checkpoint_interval: 1 validated packet`
- `review_discovery_passes: 0 (Plan 0371 discovery ledger inherited)`

## Definition Of Done

Close this plan only when the regression passes through both selector paths,
the exact committed build is installed with parity, the required browser stays
stopped, and the next Experiment 5 retry is blocked only on the user's explicit
`go` plus a fresh no-drift readiness check. A browser launch or submission is
not completion of this repair plan and remains outside its authority.
